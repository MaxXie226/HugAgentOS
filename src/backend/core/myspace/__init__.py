"""「我的空间」与沙箱之间的一致性引擎。

用户网盘的真源是 ``artifacts`` 表（界面上看到的每个文件都是一条记录），而沙箱里的
``/myspace`` 是磁盘上的 ``storage/myspace_cache/{uid}/``。两边同一份内容、两套记账，
本包负责让它们始终一致：

- :mod:`.mirror` —— 判定与搬运：磁盘上的某个文件相对账本处于什么状态（没登记过 / 改过 /
  已是最新 / 用户已删的残留），以及两个方向的实际搬运。
- :mod:`.watcher` —— 触发：镜像目录里发生任何写入或删除，都由它登记回账本。登记不再
  依赖"哪个工具动的手"，所以没有"又漏了一个入口"这回事。
- :mod:`.sandbox_sync` —— 沙箱的 ``/myspace`` 不在本机（script_runner / cube）时，把沙箱
  里的现状搬进镜像目录，好让上面那条路照常成立。它只搬运，不判断也不登记。
"""
